<div class="container">
  <form action="/expenses/{{ record.id }}?_method=PUT" method="POST" class="mt-5 needs-validation" novalidate>
    <div class="form-group row">
      <label for="staticHeading" class="col-sm-2 col-form-label">*必填</label>
      <div class="col-sm-5">
        <input type="text" readonly class="form-control-plaintext" id="staticHeading" value="請輸入你的開支">
      </div>
    </div>
    <div class="form-group row">
      <label for="inputName" class="col-sm-2 col-form-label">名稱 *：</label>
      <div class="col-sm-5">
        <input type="text" class="form-control" id="inputName" name="name" value="{{ record.name }}" required
          pattern=".*\S+.*">
        <div class="invalid-feedback">
          請輸入有效資料
        </div>
      </div>
    </div>
    <div class="form-group row">
      <label for="inputDate" class="col-sm-2 col-form-label">日期 *：</label>
      <div class="col-sm-5">
        <input name="date" value="{{ record.date }}" id="datepicker" width="276" />
      </div>
    </div>
    <div class="form-group row">
      <label for="inputCategory" class="col-sm-2 col-form-label">類別 *：</label>
      <div class="col-sm-5">
        <select class="form-control" id="inputCategory" name="categoryId" required>
          {{#each allCategories}}
          <option value="{{ this.id }}" {{#if_eq this.id }}selected{{/if_eq}}>{{ this.name }}</option>
          {{/each}}
        </select>
        <div class="invalid-feedback">
          請選取類別
        </div>
      </div>
    </div>
    <div class="form-group row">
      <label for="inputAmount" class="col-sm-2 col-form-label">金額 *：</label>
      <div class="col-sm-5">
        <input type="number" min="1" class="form-control" id="inputAmount" name="amount" value="{{ record.amount }}"
          required>
        <div class="invalid-feedback">
          請輸入有效資料
        </div>
      </div>
    </div>
    <div class="form-group row">
      <div class="col-sm-2"></div>
      <div class="col-sm-5">
        <button type="submit" class="btn btn-success mx-3">確認修改</button>
        <a href="/" class="btn btn-secondary mx-3">返回</a>
      </div>
    </div>
  </form>
</div>

<script>
  $('#datepicker').datepicker({
    uiLibrary: 'bootstrap4'
  });
</script>
<script src="/javascripts/validation.js"></script>